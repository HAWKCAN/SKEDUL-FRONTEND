export default function ReservasiButton({ type }) {
  return (
    <button
      type={type}
      className="border w-[100px] h-[30px] font-bold flex justify-center items-center rounded-md p-5"
    >Reservasi</button>
  );
}
