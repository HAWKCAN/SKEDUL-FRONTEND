export default function DetailButton({ type }) {
  return (
    <button
      type={type}
      className="border w-[100px] flex justify-center items-center font-bold h-[30px] rounded-md p-5"
    > Detail</button>
  );
}
