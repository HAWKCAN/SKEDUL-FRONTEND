export default function DetailButton({ type }) {
  return (
    <button
      type={type}
      className="border border-[#2b3284] w-[100px] flex justify-center items-center font-bold h-[37px] rounded-md  text-[20px] hover:bg-[#c5d8ff]"
    > Detail</button>
  );
}
